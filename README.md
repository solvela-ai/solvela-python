# solvela-python

> **This SDK has moved.** Source now lives in the Solvela monorepo:
> https://github.com/solvela-ai/solvela/tree/main/sdks/python

**Install (unchanged):**

```bash
pip install solvela-sdk
```

The PyPI package name `solvela-sdk` is preserved — existing installs and
`import solvela` paths continue to work without changes.

Issues, PRs, and discussion: https://github.com/solvela-ai/solvela/issues

The monorepo's `sdks/python/` is byte-equivalent to this repo's `main` at
archive time, with `pyproject.toml` `[project.urls]` rewritten to point at
the monorepo.
